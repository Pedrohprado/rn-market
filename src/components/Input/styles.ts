import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    backgroundColor: '#FFF',
    paddingHorizontal: 16,
    height: '100%',
    borderRadius: 8,
    borderWidth: 1,
    borderColor: '#C3C5CB',
  },
});
